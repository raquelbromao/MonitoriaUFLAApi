<!-- views/pages/about.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>UFLA Monitoria API</title>
  <link rel="stylesheet" type="text/css" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
  <style>
    table {
        font-family: arial, sans-serif;
        border-collapse: collapse;
        width: 100%;
        text-align: center;
    }

    td, th {
        border: 1px solid #dddddd;
        text-align: left;
        padding: 8px;
    }

    tr:nth-child(even) {
        background-color: #dddddd;
    }
  </style>
</head>
<body>
  <header>
    <% include ../partials/navebar %>
  </header>

   <main>
     <div class="container">

       <div class="jumbotron listaMonitores">
        <p>Lista de Monitores</p>
        <table>
          <thead>
            <tr>
              <th>Nome</th>
              <th>Matrícula</th>
              <th>E-mail</th>
              <th>Monitoria</th>
              <th>ID</th>
              <th>Editar</th>
              <th>Deletar</th>
            </tr>
            <% monitores.forEach(function (monitor) { %>
            <tr>
              <th><%= monitor.nome %></th>
              <th><%= monitor.matricula %></th>
              <th><%= monitor.login %></th>
              <th><%= monitor.materiaMonitorada %></th>
              <th><%= monitor._id %></th>
              <th><a class="btn btn-warning" href="/monitores/editar/<%= monitor._id %>">Editar</a></th>
              <th><a class="btn btn-raised btn-danger" onclick="return confirm('Tem certeza que deseja excluir o monitor? Esta ação não poderá ser desfeita!');" href="/monitores/deletar/<%= monitor._id %>">Deletar</a></th>
            </tr>
            <% }); %>
          </thead>
          <tbody>
          </tbody>
        </table>
      </div>

     <div class="jumbotron cadastroMonitores">
        <p>Cadastro de Monitores</p>
        <form action="/adm/monitores" method="post" class="form-horizontal">
          <div class="form-group">
            <label>Nome:</label><input type="text" class="form-control form-control-sm" name="nome">
          </div>
          <div class="form-group">
            <label>Matrícula:</label><input type="text" class="form-control" name="matricula">
          </div>
          <div class="form-group">
            <label>Login (e-mail):</label><input type="email" class="form-control" name="login">
          </div>
          <div class="form-group">
            <label>Senha:</label><input type="password" class="form-control" name="senha">
          </div>
          <button type="submit" class="btn btn-default">Cadastrar</button>
        </form>
    </div>

    </main>

  <footer>
  </footer>
</body>
</html>
